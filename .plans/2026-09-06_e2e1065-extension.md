# E2E1065 parent-linked extension — 2026-09-06

Status: implementation approved; mandatory parent join applies only to live evaluation. No live evaluation, inference, service operations, or installation authorized in this task. Extension budget starts at manifest initialization, including smoke/setup; runtime parameters must be observable before measurement.

## Scope

Only the research runner, its dedicated test file, this plan, the matching session/documentation files, and the new `artifacts/research/2026-09-05/full-e2e1065-selective-second-pass/extension-repair-01/` directory may be written. Historical attempts, production, champion, and shared runners remain immutable.

## Known context and constraints

- User reports attempt-04 retained 782 complete pairs, zero errors, hard cap at 24967 seconds. Actual context was 40960 despite historical manifest requirement 8192; retain and explicitly disclose this deviation. These artifact facts have not yet been independently verified.
- Current runner supports smoke/run and intentional-boundary resumes only; no legitimate parent-linked extension CLI exists. Do not reset/copy parent checkpoint timing/status or substitute historical hashes.
- Existing started_at validation must remain intact. New segment stores only its own evidence and timing, not inherited pairs with fabricated/null timestamps.

## Implementation and validation sequence (completed offline)

1. Read parent manifest/checkpoint/pair evidence and the six existing research suites. Confirm the 782 unique, complete, error-free pairs are exactly the frozen schedule prefix, including AS/SA order and record-to-checkpoint consistency.
2. Add fail-first regressions and execute them before implementing the extension: corrupted parent evidence; manifest/checkpoint/pair hash mismatch; non-prefix, duplicates and order drift; invalid/non-finite/out-of-range budgets; deadline reset; runtime drift; distinct historical versus measured runner/test hashes; smoke-only initialization; no smoke double counting; immutable parent verification on each resume and at end.
3. Add an isolated parent-linked continuation mode. Pin parent manifest, checkpoint and all pair hashes. Preserve old runner/test hashes separately from newly measured hashes, without asserting equivalence. Strictly inherit data/model/source357, package versions, cache and policy gates. Validate the source checkpoint linkage as well as source manifest linkage.
4. Record a new common A/S runtime contract: localhost port 18081, ctx40960, batch2048, ubatch2048. Verify actual runtime using an acceptance-approved observable source, failing closed if unavailable; endpoint/model metadata alone does not establish server parameters. Disclose historical context deviation without rewriting it.
5. Freeze a separate finite positive budget, at most 10800 seconds, independent of the parent hard cap. Persist an immutable deadline anchor and cross-check it on resume; specify with parent whether smoke/setup consumes this budget. Never refresh the deadline on resume. Bound foreground chunks to at most 3300 seconds with existing checkpoint safety.
6. Require four balanced production-parity smoke pairs before main measurement. Preserve independent full retrieval/search per arm and frozen S policy. Main starts at frozen index 782; smoke never enters the denominator. Decide with parent whether repeated smoke-only commands are rejected or safely idempotent.
7. Provide offline verified summary mode. Join only verified unique parent and new evidence; report separate parent/new quality and latency plus combined descriptive paired metrics, 10000 bootstrap draws seed42. Explicitly state unknown runtime continuity, not independent holdout, no promotion. Incomplete completion remains visibly incomplete against 1065.
8. Run dedicated regressions and all six existing research suites with actual commands, preserving all assertions. Record observed counts (121 existing tests is user-provided, not yet independently verified). Do not run live evaluation.
9. Document exact implemented smoke/run/resume/summary commands, acceptance results, changed files and blockers for the standard executor. Commands must not imply availability until the CLI and tests pass.

## Required parent join

Obtain explicit join confirmation before live evaluation only. Implemented decisions: initialization budget anchor includes smoke/setup/pauses; local listener argv is the observable runtime evidence, not a loaded-weight equivalence proof; output is extension-repair-01, with no attempt-05 directory. Offline validation completed: 156 tests pass across all six suites, including 35 new checks. See the matching session and docs files for exact commands and execution prerequisites.