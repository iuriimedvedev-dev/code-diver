# R1 selective second-pass readiness — 2026-09-05

## Status and authorization

**Latest superseding authorization and execution: LIVE192 A/S completed; JOIN before independent QA/final decision.** Parent authorized same scopes, original frozen192 cases/labels/order, independent fresh first CE passes on saved pools and fresh A/S retries. Whole production search retained; only selective second-pass hook changes. No live C, retrieval refresh, policy tuning, production/champion/services changes or promotion.

Implemented in owned runner/tests only, with new attempt artifacts and English report/session.34 tests passed; recorded adapter/actual-production parity192/192 before live; first2 fresh smoke passed without duplication; fresh baseline production replay192/192. All192 pairs and603 requests completed with no errors/timeouts. Runtime1164.089s,1166.335s including archive, below4200s; request60/30s,4170s inference alarm, ETA every12. Original schedule96 AS/96 SA; manifest freezes runner/tests/policies/source hashes before results.357 original hashes checked pre/post; strict metadata except authorized timestamp-only drift, no loaded-model equivalence assertion.

Results: net hits0, ΔMRR0/95CI[0,0], guards40 unchanged; whole-rerank p95 reduction31.654% (5.279944→3.608624s). All prespecified descriptive gates pass, not a deployment safety proof. Timer includes first/second passes, selection and meta/preservation; same setup procedure separately measured. Fixed-pool benchmark only, no retrieval/E2E claim. Fresh scores drift despite unchanged quality; no retuning. Dataset192 non-independent, prior random control does not establish evidence-selection superiority.

Retention: `live192-attempt-1/`, archive `live192-attempt-1.tar.gz` SHA256 `e939f533cf7c798e5c271ffcf531b13dccb294390394ebf7a0c99b641f14e76f`;203 payload hashes and204 archive files verified, earlier196+27 artifacts intact. Supplemental validator replays fresh S192 without network and records slice budgets/micro recall/drift/descriptive latency CI. This is internal validation, not independent QA. **Parent owns next QA/promotion and durable retention decisions.** Remaining entries are historical.

Latest superseding decision: accept only `data[*].created` as explicitly recorded runtime-instance drift, not model equivalence. Strict other metadata and all357 hashes/config links passed. Original metadata retained; loaded-model mapping remains unproven by local GGUF hashes. The same frozen12 schedule completed in `live-smoke-attempt-3/`:12/12 cases,36/36 requests,31.708s,0 errors/timeouts; parity12/12 and28 tests passed first. Both blocked attempts remain separate and intact; attempt2 stopped at schedule JSON-key representation comparison before POST, fixed without changing decisions or tolerance.

Recorded/fresh A score drift is real (78/149 nonidentical, mean absolute0.0001412403/max0.0015124083); all paired hit/MRR/recall deltas0 for recorded/S/C, with12 pairs each. No post-hoc materiality threshold, equivalence or safety proof. Retry HTTP p95 A2.22987/S0.90828/C1.17693 seconds is not whole-rerank/E2E speed evidence;15% gate does not apply. No tuning, promotion, service changes, live192 or independent QA. All27 retained attempt JSON hashes and schedule/runner/test linkage checked. **JOIN now: parent decides next evaluation/QA gate.** Historical statuses below are superseded.

Latest: parent authorized bounded live12 only. Implemented and attempted after15 passing narrow tests; selected12 exact baseline parity passed. **BLOCKED** before any retry POST (0/12): served-model metadata `data[0].created` changed1788617350→1788622626; all recorded file/model/config hashes match, other model metadata match, health OK. Do not infer changed weights or waive timestamp drift without parent decision. Raw preflight/service/schedule/summary/hashes retained under `selective-second-pass/live-smoke/`; runtime2.092s. No live192, independent QA, tuning or service changes. JOIN required.

Frozen protocol: first12 original schedule IDs with nonempty selective proper subset, no labels/outcomes; balanced six ASC permutations twice. Replay first scores, live A/S/C retry documents only, then production downstream with fresh retry scores.30s request timeout,590s hard alarm within600s. Counts/chars, common-document score drift, paired quality and CE retry HTTP p50/p95 only; no15% whole-rerank gate, E2E claim or safety proof. Current block leaves live metrics unavailable. Historical status below describes the prior offline stage.

**OFFLINE IMPLEMENTED; JOIN with parent before QA or live.** Parent subsequently authorized the new offline runner/tests/artifacts/report/session scopes below. H91a remains champion; no production, service, configuration or legacy evidence changes. All192 exact baseline JSON parity passed before frozen-policy scoring;12 narrow tests passed; final offline run6.003s. See `docs/research/2026-09-05_selective-second-pass.md` and `artifacts/research/2026-09-05/selective-second-pass/verified/` for results and hashes. Historical readiness rationale below is retained; later authorization and decisions in this status supersede its pending wording.

Approved gates: net paired hit delta>=−1/192 with separate wins/losses; primary paired percentile95CI seed42/10000 with lower ΔMRR>=−0.01; guards40 net hit loss0 and mean ΔMRR>=−0.01 exploratory. Future endpoint is rerank walltime with shared retrieval, no E2E claim. S/C pass these conditional offline gates, N fails primary MRR CI. No policy retuning and no selective advantage over control observed. Parent defines next live gate; no live mode was implemented or run.

## Evidence available

- Read `scripts/research_large_search_eval.py`, production second-pass/document/finalization logic, both research reports, and retained large-search-eval metadata. `checkpoint.json` contains 192 complete pairs: A has 6,528 first-pass documents/scores, 1,061 retry documents/scores across 121 cases; 71 cases have no retry. Pools contain full indexed content, item IDs/metadata/base scores, plus per-pair fan-in. 5,786/6,528 indexed contents exceed 850 characters; 3,362 exceed 2400 (maximum 11,997). These are inventory counts, not selective-policy outcomes.
- Manifest retains queries, slice-specific labels, ordering, resolved configuration and dependency/model/data/code hashes. Arms retain ordered requests, local score indices, complete outputs, meta features/predictions, traces and timings. Per-pair files, journal, parity smoke, integrity analysis and evidence archive exist. Existing QA reports complete recomputation; this readiness pass did not independently repeat it.
- Baseline A only is reusable. B fragments have different first batches and retry decisions. Older `ce-evidence` R1 contains one completed first/selective case and an interrupted budget control, uses raw CE ranking without production meta/preservation, and cannot fill missing H91a scores.
- Expanded baseline candidates are scored only inside A's recorded retry set. Scores for other expanded candidates do not exist. Actual served token counts are absent. Current model/hash availability must be checked before replay; local file hashes do not attest served weights or immutable index state. Evidence is gitignored; parent must arrange durable retention.

## What replay can establish

1. **Exact recorded baseline reproduction:** replay A's identical query, ordered first/retry documents, `top_n`, score-list order and index mapping through production search, adjustments, freshly recomputed meta features/model predictions and final preservation. Require complete output and feature parity, not just hit metrics. Exclude wall-clock trace fields from equality. This reproduces a recorded execution, not a new live inference.
2. **Exact fixed-first-score no-retry ablation:** suppress only the second-pass hook, retaining first request and configuration (including full-score `top_n`). Recompute downstream stages. This exactly measures removing retry conditional on recorded first scores, not independent live behavior or latency.
3. **Conditional selective/control screening:** retain selected members' historical second scores and merge `max(first, second)`. This is exact bookkeeping under a frozen-score assumption, **not exact live selective replay**: changing batch membership, length, order or `top_n` can change scores. Even unchanged document bytes do not establish batch invariance. Identical full requests can use exact replay; changed batches must be marked conditional per case. Never substitute first scores for missing expanded scores, use B scores, or silently shrink the evaluation population.

Keep original pools/base scores immutable; isolate arm providers, feature caches, configuration, fan-in and Booster. Do not reuse saved meta predictions under changed CE scores or replace production finalization with raw CE sorting. Fail closed on hash/request/model mismatch or silent rerank/meta fallback. Research-only override of `_with_second_pass` is the narrow policy seam; production code stays unchanged. Baseline delegates to the unmodified method. Preserve its ordering nuance: sub-floor scores retain provider order unless cap overflow triggers base-index sorting; merge sorting is stable.

## Freeze policies before reading their quality outcomes

- **A:** production floor strictly `<0.3`, cap24, content850→2400, unchanged merge/meta/preservation. Let T be its exact selected retry indices, including original order.
- **N:** no retry, fixed first request as above.
- **S (cheap selective):** filter T only; no refill from beyond the cap. For indexed content c, require `len(c)>850` and a query term occurring wholly inside c[850:2400] that is absent from c[:850]. Use the frozen runner's CamelCase splitting, lowercase alphanumeric tokens and STOP list; define token spans against complete c to avoid boundary-created partial tokens. Empty query-term set means no retry. Preserve T order. Evidence after2400 alone cannot justify this retry. Inspect indexed content, not filesystem source: production default expands the indexed prefix, not a new source fragment. No labels, AST, semantic model or threshold search.
- **C (deterministic random matched-budget control):** sample from T without replacement, independent of term evidence/labels, matching S's per-query retry count. Predeclare rendered-expanded-length bins of256 characters and match S's count in each bin; order candidates by SHA256 of seed42/case ID/candidate index, take the required count, then restore T order. No seed sweep or redraw. Retain overlap, identical-set cases and realized character gaps. Flag gaps exceeding5% of S retry characters as unmatched; report rather than relabel/drop them. Empty S gives empty C. This constrained T control tests evidence selection within baseline eligibility, not random selection across all34.

Count equality and comparable rendered characters are **not exact token equality**, equal compute or measured speed. Character matching may provide little contrast when T is small; report effective contrasting cases. Freeze constants, tokenizer rule, control construction and manifest before outcome evaluation; never tune using labels or R2 losses.

## Minimal staged screening and future scopes

All following writes/runs require new parent authorization:

1. New `scripts/research_selective_second_pass.py`: offline-only default, no production/service construction; strict artifact loader, policy/replay adapters, parity, screening and explicit separately gated live mode. Reuse frozen runner helpers without editing it or invoking its live entry points.
2. New `tests/test_research_selective_second_pass.py`: full baseline request/output/features parity; zero retries, strict floor equality, cap overflow/provider ordering, index remapping/ties/max merge; 850/2400 boundaries, absent/new/repeated terms, empty text/query; deterministic character controls; unchanged high CE scores versus potentially changed ranks; preservation/meta recomputation, invalid/missing scores, mismatch and injected retry failure. Require network-denied offline execution and fail on unexpected calls. Tests first, then representative recorded parity, then all192 baseline parity **before** policy outcomes.
3. New `artifacts/research/2026-09-05/selective-second-pass/`: immutable policy/input hash manifest, request/index coverage inventory, parity results, per-case decisions/reasons/term spans, selected indices and document hashes, hypothetical merged scores, recomputed features/predictions/output, budget tables and labeled exact/conditional summary. Keep legacy artifacts immutable. No offline timing column masquerading as CE savings; report requests/docs/chars avoided only.
4. New `docs/research/2026-09-05_selective-second-pass.md`: evidence classification, all prespecified arms including negative results, missing coverage, slice metrics and limitations. New `.session/2026-09-05_selective-second-pass.md`: compact decisions, authorization, hashes, outcomes and next JOIN. Parent arranges durable artifact copy/checksum outside disposable worktree.
5. **JOIN offline results before any live smoke.** If worthwhile, authorize a small deterministic, label/outcome-blind batch-sensitivity smoke spanning zero retry, filtering, cap saturation and short/long documents. Compare actual full versus subset requests and record common-document score/rank drift; confirm production parity and fallback. Smoke cannot prove universal batch invariance. Stop for integrity/fidelity errors; no retuning after smoke outcomes.
6. Only then consider new paired live192 under an explicit4200-second/70-minute campaign deadline, sequential exclusive CE ownership, same pool per pair, fresh first/second calls, isolated arms and counterbalanced order. Choose the prespecified finalist/comparisons before launch; budget all controls explicitly rather than assume a multi-arm run fits the earlier two-arm40.48 minutes. ETA checkpoints and durable per-call/partial-pair journaling; no automatic continuation. Separately cap/record smoke time. No service changes.

## Metrics and decision gates for parent confirmation

Report exact-file hit@10, MRR@10, macro/micro recall@10, paired deltas/wins/losses/ties, lost/rescued hits, pool-gold coverage and retry buckets0/1–8/9–16/17–24. Fixed denominator192 with explicit failure/missing-case bounds; slices use their own labels: WHERE78, novel95, non-WHERE mechanical40, selected mechanical118. Do not add overlapping slices. Report191 normalized-query and190 no-glob sensitivities; descriptive seeded10,000 paired bootstrap95%CIs plus query/shared-answer cluster sensitivity (previous evidence:179 components). Label-informed sampling and historical model overlap mean192 is **not independent validation**.

Provisional parent gates versus A: hit loss≤1/192 (report both lost-case count and net hit delta; confirm intended interpretation), lower95%CI of ΔMRR≥−0.01 for noninferiority. Freeze the primary CI/cluster rule with parent before scoring; do not choose whichever interval passes. Slice regressions remain visible; no critical-guard tolerance is silently invented. Conditional offline results can prioritize or reject further spending, not certify deployment noninferiority.

Speed requires **real measured p95 gain≥15%**, with endpoint fixed before live work (propose rerank wall including selection/document building/meta/preservation; report CE and retrieval separately). Report paired latency distribution, uncertainty, warm/order strata, realized calls/docs/chars and actual tokens only if available. Do not subtract historical retry time or scale character counts to simulate latency. Shared-retrieval reconstructed E2E is not standalone E2E. Full-batch execution with selectively masked scores can establish score-ablation quality but offers no reduced-batch speed evidence. Final verdict/promotion belongs to parent; champion remains unchanged.