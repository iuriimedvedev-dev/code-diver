# MLX runtime setup — 2026-09-08

## Scope

Install released `mlx` and `mlx-lm` for native Apple Silicon in an isolated
environment at `.tmp/mlx-runtime/`, with model artifacts confined to
`.tmp/mlx-model-cache/`. Do not modify the root `.venv`, `uv.lock`, project
configuration, services, or application code. This setup does not provide or
integrate an HTTP reranker endpoint.

## Selected runtime

- Interpreter: `/Users/iurii.medvedev/.local/bin/python3.12`
- Verified: CPython `3.12.12`, native `arm64`, pip `25.3`
- Isolation command: `python3.12 -m venv .tmp/mlx-runtime`

## Execution and verification

1. Create the isolated venv and set `HF_HUB_CACHE` to
   `.tmp/mlx-model-cache/huggingface`.
2. Upgrade only packaging tools in the isolated venv, then install released
   `mlx` and `mlx-lm` from PyPI.
3. Run a finite inline packaged-API smoke test that records package versions,
   `mx.default_device`, `mx.metal.is_available()`, supported GPU memory
   statistics, a finite array forward operation, and synchronized timing.
4. Use the packaged `mlx_lm` loader APIs to download/load
   `mlx-community/Qwen3-Reranker-0.6B-4bit` into the project cache and run a
   small finite forward computation. Record the exact model revision if the
   Hub exposes it; if download/load fails, preserve the exact error while
   retaining independent runtime evidence.
5. Save exact repeatable commands, versions, measurements, and final status in
   `docs/research/2026-09-08_mlx-setup.md` and `.session/2026-09-08_mlx-setup.md`.

## Definition of done

- Isolated `mlx` and `mlx-lm` installation succeeds or its exact failure is
  documented.
- MLX executes a finite synchronized computation on the Metal GPU, with
  timings and memory/device evidence recorded.
- The requested model is cached and loaded with a finite forward smoke test,
  or its exact download/load error is documented separately from runtime
  success.
- No HTTP adapter, scorer, source script, service, or application edit is
  created; integration remains a parent-level readiness decision.