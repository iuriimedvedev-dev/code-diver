# MLX setup session — 2026-09-08

## Completed

- Created isolated `.tmp/mlx-runtime/` with native arm64 CPython `3.12.12`.
- Installed released `mlx==0.32.2`, `mlx-metal==0.32.2`, and
  `mlx-lm==0.31.3`; the root `.venv` remains without MLX packages.
- Confirmed `mx.metal.is_available() == True` and default device
  `Device(gpu, 0)`.
- Confirmed a finite synchronized `(1024, 1024)` MLX GPU computation. The
  supported post-forward counters were active `12,582,928` bytes, cache
  `3,146,440` bytes, and peak `15,728,796` bytes; synchronized time was
  `0.003064792` seconds.
- Downloaded and loaded `mlx-community/Qwen3-Reranker-0.6B-4bit` through
  packaged `mlx_lm.load` into `.tmp/mlx-model-cache/huggingface/`.
- Verified model revision
  `5f324548f1d20c2b5a450f126fc6ef2fb1126524`, `346,771,529` bytes total, and
  finite `bfloat16` forward output shape `(1, 16, 151669)` in
  `1.027761625` seconds.

## Boundaries

- No source scripts, scorer, HTTP endpoint, service, application edit, root
  `.venv` change, `uv.lock` change, or project configuration change was made.
- Runtime/model setup is ready for a parent-level integration decision; the
  reranker HTTP API remains **not integrated**.

## Reproduction

The exact installation and inline runtime/model commands, package versions,
memory statistics, model revision, and output measurements are recorded in
`docs/research/2026-09-08_mlx-setup.md`. The setup plan is in
`.plans/2026-09-08_mlx-setup.md`.