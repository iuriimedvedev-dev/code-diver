# Bounded Rust parity fixes — actual execution

- Scope: edited existing untracked `native/code_diver_search_bin/src/pipeline.rs`, `types.rs` (configuration and rank comments), and `main.rs` (CLI wiring). No Python, model, dependency, or fusion.rs edits; preserved preexisting working-tree changes. No commits or live service/benchmark runs.
- Shared `prepare_candidates` now supplies search, single feature export, and batch feature export. Shared `candidate_features` supplies inference and export's 16 features. Removed divergent export scoring and single-export-only hub band. Both exports include `time_s` (not a model feature).
- Finalization uses merged second-pass scores for logit/tie sorting, then updates both `ce_rank` and `base_fused_rank` to adjusted CE order. Original `ce_index` remains available for capped retry selection. Existing max(first, second) merge and warning/fallback on second-pass errors retained.
- New `--retrieval-limit` defaults to 360 independently of `--candidate-limit` (34). Selection keeps the strongest fused representative per exact path before CE truncation, with deterministic path/item-id fallback for fused ties. This is deduplication, not Python's full file aggregation.
- Regression command: `cargo test --manifest-path native/code_diver_search_bin/Cargo.toml pipeline::tests -- --nocapture`. Before ordering fix: 0 passed, 2 failed (first.rs remained before rescued.rs; first-pass score prevented final-score tie break). Intermediate run: ordering tests passed; capped retry and duplicate-slot tests failed (2 passed, 2 failed). Both subsequently fixed.
- Final command: `cargo test --manifest-path native/code_diver_search_bin/Cargo.toml`. Exit 0; 35 passed, 0 failed, 0 ignored, 0 filtered; seven new pipeline tests. Includes rank feature slots, logit/ties, retry floor/cap, empty/zero windows, deterministic selection, duplicate paths. Four compiler warnings (unused imports/mut and dead code); no weakened tests.
- Release: `cargo build --release --manifest-path native/code_diver_search_bin/Cargo.toml`, exit 0, optimized build finished in 2.43s, three warnings (unused main HashSet, dead coverage helpers). Binary: `native/code_diver_search_bin/target/release/code_diver_search_bin`. SHA256: `7c54b8750d4c327a19b8045577837eac09e784d61658d76bf0033ed1efcf9724`. Executed `--help` successfully and verified new flag/default.
- QA CLI: binary with `--catalog <catalog.jsonl> --graph <graph.jsonl> --model <model.txt> --base-path <repository> --retrieval-limit 360 --candidate-limit 34`; search adds `--query <query> --limit 10`; single export adds `--query <query> --dump-features --query-id <id> --expected <comma-separated-paths>`; batch export adds `--dump-features-file <queries.jsonl>`. Service overrides: `--embedding-url`, `--qdrant-url`, `--ce-url`. Do not start live evaluation before parent join.
- Outstanding: independent lexical/path/symbol admission and Python file aggregation/votes remain absent; graph-enabled behavior not made equivalent; fan-in lookup in untouched features.rs remains constant 1 per catalog path, not true graph fan-in. Hub fallback remains unimplemented. Document construction and feature parity are not certified. Existing generic result `score` still takes max(meta, CE, fused). No retraining, quality measurements, or service-backed entry-point comparison performed; common-path consistency is structural plus unit-tested ranking/features.

## 2026-09-07 paired diagnostic result

- Documentation: full evidence is in `docs/research/2026-09-07_rust-parity-diagnostic.md`; no code or runtime artifacts were changed by the diagnostic.
- Native release/artifacts: binary SHA-256 `7c54b8750d4c327a19b8045577837eac09e784d61658d76bf0033ed1efcf9724`; catalog `e32b2d12539b914bdff33a4d1935d4d4597ac43940f6925d450519f9222cbf3e`; graph `4c89a44094bbc6c71a2d34751e7dabeb9f5c7685719051f1876b743dfa6f2cf3`; model `8dcadfdc02b050fd35ebafca7f436c822859ff38cfeba4f9bd1203abc90e5010`.
- Exact `configs/intellij/intellij-h91a-meta-ranker.yml` was used; read-only comparison with `intellij-h91a-champion.yml` was `CONFIG_CMP=identical`. Effective endpoints were embedding `localhost:8001`, Qdrant `localhost:6333`, CE `localhost:18081`; native settings were retrieval `360`, candidate cap `34`, result limit `10`, with no base path.
- All `12/12` arms completed with zero failures across `where-daemon-analyzer`, `where-debugger-session`, and `where-gradle-import`, each repeated twice with seed `42` pair ordering. Python: Hit@10 `1.000000`, MRR `1.000000`, wall mean `6933.474 ms`; Rust: Hit@10 `1.000000`, MRR `0.833333`, wall mean `11651.515 ms`, native internal mean `4698.215 ms`. Python initialization was `1496.120 ms` and excluded from per-search wall.
- The observed MRR gap comes from the daemon case: Python rank `1` in both repeats versus Rust rank `2` in both repeats. This is three known cases repeated twice, not six independent cases; do not claim parity, precision@10, speedup, or warm p95.
- Top-10 list computation from the reported rows gives `43` unique paths across the six distinct arm/case lists (`30` Python, `30` Rust slots, `17` cross-arm duplicate slots); this is descriptive only.
- Outstanding remains independent lexical/path/symbol admission, Python aggregation/votes, graph fan-in equivalence, a sealed independent holdout, and a comparable persistent-workload latency measurement.