# Code Diver — Delta Audit + Code Review (2026-06-04, batch `a748ac4..HEAD`)

> Status addendum after follow-up commits on 2026-06-04:
>
> This file is a dated audit snapshot, not the current state of the working tree.
> The original blocker is resolved and the current suite is green: `319 passed`.
> The MTEB qrel inflation finding (V-9) is fixed by selecting `score > 0` qrels before
> the benchmark limit and is covered by `test_mteb_preparer_selects_positive_qrels_before_limit`.
> The runtime log FD leak (V-1/CR-1) is fixed with `finally: log.close()`, and
> `GLOO_SOCKET_IFNAME` now uses `lo0` on macOS and `lo` elsewhere. `cli.py` has grown
> further to 2,169 lines. Open items in this audit should be rechecked against HEAD
> before using them as a current remediation list.

**Scope:** 14 commits, **+4,499 LOC (+35%)** → 234 files, ~17.2k LOC, `cli.py` now **1,811 LOC**.
New `runtime/` package (vLLM/Metal venv setup + model download + server lifecycle), `benchmarks/`
package (MTEB CodeSearchNet), embedding profiles, `init` wizard, **`search` now launches a
code-exploration agent by default**, progress UI. Method: three parallel read-only agents —
correctness audit (findings **V-**), code review, QA test run. Builds on `AUDIT.md`,
`AUDIT-2026-06-02.md`, `AUDIT-2026-06-02-tracing.md`.

---

## 🔴 Blocker — working tree is broken right now

`cli.py:530` has a malformed `finally:` block (body not indented) → **`IndentationError`**. The
committed HEAD compiles (287/287 tests pass), but the **uncommitted working tree fails collection
of 42 tests across 9 modules** (`test_cli_*`, `test_benchmark_profiles`, `test_postrank_*`,
`test_inspection_cli`, `test_graph_strategy`, …). An early test run only "passed" off a stale
`.pyc`. **Fix the indentation before committing/CI** — nothing else matters until the tree parses.

---

## Verdict

> The batch is **well-engineered feature work with real bug fixes** — the three highest-priority
> open items from prior audits (T-5 trace corruption, T-2 partial eval loss, I-1W partial index)
> are now genuinely fixed and covered by strong concurrency tests. But it adds **+4,499 LOC, a new
> supply-chain surface, a benchmark-inflation risk, and a silent offline-search regression**, and
> the lean-`{index,search,eval}` cleanup is still not happening — `cli.py` *grew* again and gained
> *new* metric duplication. The project keeps getting richer, not simpler.

## Claimed fixes — verified

| Prior ID | Status | Evidence |
|----------|--------|----------|
| **T-5** trace interleave | ✅ **FIXED** | `_artifact_locks` classvar + `_artifact_locks_lock`; `test_trace_logger_instances_share_artifact_lock` (24×60 KB concurrent writes, no corruption) |
| **T-2** parallel eval loses partials | ✅ **FIXED** | `rows_by_index` + `_failed_case`; `evaluation_completed` always emitted; `degraded_cases` populated |
| **I-1W** Qdrant partial write | ✅ **FIXED** | staging-collection + rollback in `replace_batches`; rollback test exists |
| transient retry | ✅ **FIXED (generation only)** | `transient_generation_retry.py`: bounded, backoff+jitter, `Retry-After`, typed classifier; 3 tests |
| **T-1** `_item_profiles` lock | ⚠️ **still open** | guarded only when `profile_lock` is passed; **`None` by default** |
| **I-1/I-10** embedding retry | ⚠️ **partial** | generation wrapped; **OpenAI-compatible (local vLLM) embedding has no retry** — one 503 aborts the whole index (now the *default* path) |
| **S-1a** metric parity | ❌ **still open & confirmed** | `cli.direct_search_matches` lacks the `::` symbol-id match that `EvaluationService` has → `evaluate` and `evaluate-search-tools` give different hit counts on symbol-chunked indexes; **no parity test** |
| **T-3** trace open/close per event | ❌ still open |

## New findings (ranked, prefix V- = correctness, CR = review)

| # | Sev | ID | Finding | Location |
|---|-----|----|---------|----------|
| 1 | 🔴 | (blocker) | `IndentationError` in working tree breaks 42 tests | `cli.py:530` |
| 2 | 🟠 High | V-2 | **No checksum/hash on the vLLM-metal tarball + WHL download** during `init` — supply-chain risk on a dev machine; `--index-strategy unsafe-best-match` opts out of safe resolution | `runtime/embedding_runtime_manager.py:56,80` |
| 3 | 🟠 High | V-9 | **MTEB qrels sliced by row index, not filtered to `score>=1`** — `score=0` pairs treated as positives → **silently inflated benchmark hit-rates** | `benchmarks/mteb_codesearchnet_preparer.py:23` |
| 4 | 🟠 High | V-7 | **`search` (default) now invokes `PiRunner` with no LLM/offline guard** — an offline user who had deterministic search gets a subprocess/API error; preflight only checks the vector store. (`evaluate` still uses the deterministic path — no eval regression.) | `cli.py` cmd_search |
| 5 | 🟠 High | V-1 / CR-1 | **Log file descriptor leaked** in `start()` — `open("ab")` passed to `Popen`, never closed; leaked outright if `Popen` raises | `runtime/embedding_runtime_manager.py:130` |
| 6 | 🟡 Med | CR-3 | **`GLOO_SOCKET_IFNAME="lo0"` set unconditionally** — `lo0` is macOS-only; silently wrong on Linux CUDA/ROCm | `runtime/embedding_runtime_manager.py:133` |
| 7 | 🟡 Med | CR-4 | **`CandidateFileScanner` calls 3 private `_`-methods of `CodebaseScanner`** (`_should_skip_file`, `_read_text`, `_items_for_file`) — fragile cross-class coupling; will break silently on scanner refactor | `services/candidate_file_scanner.py:25-30` |
| 8 | 🟡 Med | V-4 | **All `runtime/` paths are CWD-relative** — runtime config silently missed when CWD ≠ project root; profile silently skipped | `runtime/runtime_config_store.py:11` |
| 9 | 🟡 Med | V-15 | **`rg` vs `os.walk` fallback diverges on `.gitignore`** — files excluded by `.gitignore` get indexed only when `rg` is absent; gitignore test is `skip`ped without `rg` | `services/codebase_scanner.py:125-149` |
| 10 | 🟡 Med | V-12 | **No dimension guard on the `search` path** — querying an index with a different-dimension profile returns a silent Qdrant error/wrong results (only `append()` guards) | `config/embedding_profile_registry.py`, indexing/search |
| 11 | 🟡 Med | V-3 | Detached vLLM server (`start_new_session=True`), **no PID file / no `stop`** → orphaned process on CLI crash; no port-in-use check | `runtime/embedding_runtime_manager.py:156` |
| 12 | 🟡 Med | V-10/V-11 | Benchmark prep `rmtree`s assets **before** confirming the HF download; no network-error handling → failed download leaves no assets + raw traceback | `benchmarks/mteb_codesearchnet_preparer.py:20-29` |
| 13 | 🟡 Med | CR-5 / S-1a | **New metric duplication**: `cli.direct_search_metrics` + module-level `mean`/`percentile` reimplement `EvaluationService`/`EvaluationStatistics` (with a deferred `import math` inside the fn). cli.py grew with *more* business logic | `cli.py:1649-1694,1612` |
| 14 | 🟢 Low | CR-2 | Timestamped vLLM-metal wheel URL hardcoded inline (`vllm_metal-0.2.0-…arm64.whl`), version `0.2.0` vs source `0.22.0` undocumented — breaks on any rebuild; not a named constant | `runtime/embedding_runtime_manager.py:80` |
| 15 | 🟢 Low | CR-7 | Dead branch: `if not args.json:` after the `args.json` path already returned | `cli.py:646-652` |
| 16 | 🟢 Low | CR-11 | 16 unnamed blend-weight literals in fast-mode H3; bypass `HybridSearchConfig` → invisible to experiment tracking | `agent/h3_search_tool_handler.py:87-208` |
| 17 | 🟢 Low | V-5 | `_python_can_import` interpolates `module` into a `-c "import {module}"` string (hardcoded today, unsafe pattern) | `runtime/embedding_runtime_manager.py:94` |

Plus: new silent fallbacks — `_candidate_file_count` returns `0` on any exception (progress shows nothing); benchmark download path unguarded in `cmd_evaluate`.

## Tests & coverage (real numbers)

- **Committed HEAD: 287 passed (was 181), 0 failures, 0 errors** — +106 tests. Working tree: **broken** (blocker above), collects 245/287 with 9 errors.
- **Overall coverage 79%.** New-code gaps: **`agent/h3_search_tool_handler.py` 15%** (230 stmts, ~no behavioral tests), `benchmarks/mteb_codesearchnet_preparer.py` 24% (needs HF mock), `runtime_setup_wizard.py` 59% (interactive branches untested), `embedding_runtime_manager.py` 61%.
- **Strong new tests:** `test_trace_logger` (T-5 concurrency), `test_hybrid_candidate_scorer` (8-thread cache-lock), `test_transient_generation_retry`, `test_postrank_*`. 
- **Still-missing characterization tests:** metric **parity** (cli vs EvaluationService, the `::` bug), `_normalize` single/tied → 1.0, RRF zero-score, route weight-sum invariant.
- **Marker hygiene:** 3 `tests/unit/` files lack `pytestmark` (`test_postrank_cross_encoder`, `test_query_caching_embedding_provider`, `test_validate_eval_dataset`); the last spawns a real `uv run` subprocess (~2-3 s, machine-dependent) — misclassified as a unit test.

## Direction check

| | batch −1 | now |
|--|--|--|
| LOC | 12.7k | **17.2k** |
| `cli.py` | 1,129 | **1,811** |
| CLI surface | 16 cmds | 3 public + 13 hidden behind `--help-all` (good UX) |
| Cleanup plan (Phases 0–6) | unstarted | **still unstarted** (no composition root, metrics still in cli, +new duplication) |
| Public-surface clarity | — | ✅ improved (`{index,search,evaluate}` front-and-center) |

The **public surface** moved toward the goal (3 commands shown by default, nice progress UX, reproducible benchmark, local-runtime `init`). The **code** moved away (more LOC, more cli.py business logic, new duplication). Good product polish; the lean-architecture refactor remains entirely ahead.

## Remediation priority

1. **Blocker:** fix `cli.py:530` indentation; re-run `uv run pytest -q` to confirm 287 green.
2. **V-2 (High):** pin hashes (`uv pip install --hash` / verify sha256) on the vLLM-metal download; drop `--index-strategy unsafe-best-match`.
3. **V-9 (High):** filter MTEB qrels to `score>=1` before building eval positives — otherwise benchmark numbers are inflated and untrustworthy.
4. **V-7 (High):** add an LLM/offline guard to `cmd_search` (or keep a deterministic default) so offline `search` still works or fails with guidance.
5. **V-1/CR-1 (High):** wrap the log handle in `try/finally` (close after `Popen`).
6. **S-1a + parity test (Med, recurring):** unify the cli `direct_search_*` metrics onto `EvaluationService`/`EvaluationStatistics` and add the parity characterization test (the `::` divergence) — this is the no-regret cleanup step that's been deferred for four batches.
7. **CR-3, V-4, V-15, V-12 (Med):** platform-guard `GLOO_SOCKET_IFNAME`; resolve runtime paths to project root; align `rg`/`os.walk` gitignore behavior; add a search-path dimension guard.
8. Coverage: behavioral tests for `h3_search_tool_handler` (15%) and a mocked-HF test for the benchmark preparer.
