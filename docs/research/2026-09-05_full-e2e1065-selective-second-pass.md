# Отчет о проведении исследования Full E2E 1065 (Selective Second Pass)

## 1. Общая информация
*   **Дата**: 2026-09-05
*   **Кампания**: Selective Second Pass (`attempt-04`)
*   **Цель**: Оценка 1065 пар документов с использованием исправленного рантайма Reranker.
*   **Статус**: **Приостановлено по лимиту времени (hard_cap)**.

## 2. Параметры рантайма
*   **Эндпоинт**: `http://localhost:18081/v1/rerank` (PID 25811)
*   **Модель**: `Qwen3-Reranker-0.6B-Q4_K_M.gguf`
*   **SHA256**: `c04f5f5657c52e04538c455e8c62817db3d3b795b39e9f547f8581510445f075`
*   **Конфигурация llama-server**:
    *   `n_ctx`: 40960 (фактический runtime; в манифесте зафиксировано 8192 — protocol deviation, provenance требует отдельной фиксации)
    *   `batch_size`: 2048
    *   `ubatch_size`: 2048
    *   `backend`: Metal (MTL) / Accelerate
    *   `threads`: 12

## 3. Прогресс выполнения
*   **Smoke Test**: Пройден (4/4 пары, паритет подтвержден).
*   **Всего пар по плану**: 1065
*   **Успешно завершено**: **782** (~73.4%)
*   **Осталось**: 283
*   **Ошибок (HTTP/Model)**: 0 observed errors in 782 pairs.
*   **Затраченное время**: 24,967 секунд (6.93 часа).
*   **Среднее время на пару**: ~31.9 сек.
*   **Прогноз полного времени**: ~34,000 сек (~9.4 часа).

## 4. Метрики качества и Latency
*   **Latency p50**: A=13.0996s, S=12.1690s.
*   **Latency p95**: A=34.0640s, S=35.3395s.
*   **p95 CI**: `[-2.2180s, +5.2988s]`; интервал включает 0, поэтому ускорение не доказано, а отсутствие статистически значимой регрессии не является доказательством эквивалентности.
*   **Gate / promotion**: **INCONCLUSIVE / NO PROMOTION**. Выборка неполная (782/1065, 73.4%); наблюдаемые Hit@10 и Recall@10 совпали на обработанных срезах, но MRR@10 на `full1065` отличается (A=0.8372, S=0.8377), поэтому утверждение `all quality same` не делается.

## 5. Результаты (Handoff)
*   На новом сервере (18081) зафиксировано 0 observed errors на первых 782 парах при фактическом `n_ctx=40960`.
*   Исходная проблема HTTP 500 подтверждена для physical batch 768; необходимость `n_ctx=40960` для стабильности не доказана. Использование `40960` было сделано агентом для совместимости с metadata.
*   Все обработанные 782 пары валидны и сохранены в `artifacts/research/2026-09-05/full-e2e1065-selective-second-pass/attempt-04/pairs/`.
*   Хеш манифеста: `408068dc18a37060fd132dbe08356020b8e71918d9f261fb76cc8992b8a46ad4`.

## 6. Родительский вывод и блокер продолжения
Текущий результат — **INCONCLUSIVE / NO PROMOTION**: это partial run 782/1065 и не подтвержденный frozen full eval из-за расхождения manifest `n_ctx=8192` с actual runtime `n_ctx=40960`. Для оставшихся 283 пар требуется новый утвержденный протокол, согласование и фиксация фактического runtime provenance и временного лимита; старые manifest/checkpoint нельзя подменять.
