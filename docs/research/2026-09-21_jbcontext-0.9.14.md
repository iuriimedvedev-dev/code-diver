# code-diver vs jbcontext 0.9.14 — 2026-09-21

## Сетап

- jbcontext обновлен: **0.9.10.670 → 0.9.14** (build 916, stable channel) через
  официальный `download-jbcontext.sh`. Потребовался свежий `jbcontext login`
  (старые креды пропали вместе с `~/.jbcontext`).
- Корпус: текущий HEAD intellij-community `4756d30e` (август меряли на `693e76f0`).
  Решение: мерить на HEAD, с оговоркой про дрейф (см. ниже).
- Харнес без изменений: `scripts/compare_jbcontext.py` (те же file-level формулы,
  raw_limit 40, file_limit 10, reranker default/FAST).

## Результаты jbcontext 0.9.14 @ HEAD

| Датасет | hit@1 | hit@3 | hit@10 | MRR@10 | recall | mean | p95 | failed |
|---|---|---|---|---|---|---|---|---|
| WHERE78 (n=78) | 0.244 | 0.436 | **0.654** | **0.368** | 0.600 | 1546ms | 2008ms | 0 |
| full1065 (n=1065) | 0.379 | 0.537 | **0.637** | **0.467** | 0.610 | 1308ms | 1448ms | 1 |

## Динамика vs 0.9.10 @ 693e76f0 (август)

| | 0.9.10, WHERE78 | 0.9.14, WHERE78 | 0.9.10, 1065 | 0.9.14, 1065 |
|---|---|---|---|---|
| hit@10 | 0.782 | **0.654 (−12.8pp)** | 0.680 | **0.637 (−4.3pp)** |
| MRR@10 | 0.350 | 0.368 (+0.018) | 0.504 | 0.467 (−0.037) |
| mean | 1893ms | 1546ms | 1514ms | 1308ms |

Новая версия быстрее (−15-20% latency), но хиты ниже. **Причины смешаны**:
версия + дрейф корпуса (месяц коммитов) + возможно другое состояние индекса.
Разделить эффекты можно только прогоном 0.9.14 на пине `693e76f0`
(`--revision`, коммит доступен локально) — не сделано.

## Head-to-head vs code-diver (сентябрь)

| full1065 | hit@10 | MRR@10 | mean |
|---|---|---|---|
| code-diver H91a Python (`champion_results.json`) | **0.861** | **0.718** | 8427ms |
| code-diver Rust новый (`2026-09-21_h91afix-run21`, n=21) | 0.905 | 0.825 | 3871ms |
| **jbcontext 0.9.14** (n=1065) | 0.637 | 0.467 | **1308ms** |

| WHERE78 | hit@10 | MRR@10 | mean |
|---|---|---|---|
| code-diver H91a (`champion_results.json`) | **0.872** | **0.707** | 11831ms |
| **jbcontext 0.9.14** | 0.654 | 0.368 | **1546ms** |

Расклад сохраняется: code-diver выигрывает **+22pp hit / +25pp MRR** на 1065
(**+22pp / +34pp** на WHERE78), jbcontext быстрее в **6.4x** (1065) / **7.6x** (WHERE78).
Разрыв по хитам даже вырос: jbcontext просел сильнее, чем code-diver.

## Эвиденсы (gitignored, локально)

- `.code-diver/reports/jbcontext-where78-0.9.14.json`
- `.code-diver/reports/jbcontext-intellij-1065-0.9.14.json`
- Лог: `/tmp/jbcontext-1065.log` (1393s wall, sequential solo run)

## Следующий шаг (опционально, для строгости)

Прогнать 0.9.14 с `--revision 693e76f0a37ade9509c9edcf505c446672c9c0b1`
на WHERE78 — отделит эффект версии от дрейфа корпуса (~3 мин).
