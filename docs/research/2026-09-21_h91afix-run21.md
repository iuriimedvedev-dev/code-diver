# H91a-fix eval + CE backend shootout — 2026-09-21

## Итог

Новый Rust-бинарь `f84870ab...` (H91a parity: ручные веса fusion, флаги ранжирования,
lexical admission + file_vote, catalog-title CE-доки) закрыл большую часть гэпа
на тех же 21 кейсах (seed 42, индексы 0..20), обе руки на одном llama CE `:18081`:

| full1065 (n=21) | Python | Rust | Rust−Python |
|---|---|---|---|
| **Новый бинарь, общий CE (2026-09-21_h91afix-run21)** | 0.952 / 0.952, p50 5130ms | **0.905 / 0.825**, p50 3871ms | **−0.048 / −0.127** |
| Старый бинарь, Rust на MLX (`2026-09-09_restored-run30`) | 0.952 / 0.952 | 0.714 / 0.556 | −0.238 / −0.397 |

Гэп: hit −23.8pp → **−4.8pp (+19pp)**, MRR −39.7pp → **−12.7pp (+27pp)**.
Rust быстрее Python: p50 −25%, p95 −26% (3871/5442ms vs 5130/7394ms).
mech150-срез (4 пары): дельта 0.0/0.0 (было −0.5/−0.5). Ноль failures.

Smoke (`2026-09-21_h91afix-smoke3`, 2 пары): Python 1.0/1.0 vs Rust 1.0/0.667,
дельта hit 0.0, MRR −0.333 (один кейс rank 3 vs 1).

## CE backend: llama vs vLLM-metal (официальный шаблон решает)

`scripts/bench_ce_backends.py`, payload 34 доки (`payloads_v2.json`):

| cap | llama `:18081` | MLX `:18083` (tuned: seq32, batch16384, len4096) | top-10 overlap |
|---|---|---|---|
| 850 | **1.60s** | 2.29s | **9/10** |
| 2400 | 3.25s | **1.73s (1.9x)** | **9/10** |

Ключевая находка: со встроенным шаблоном MLX давал overlap **3/10**, mean delta 0.49.
С официальным `chat_template.jinja` из снапшота (`5f324548...`) — **9/10**, max delta
0.79/0.68 на тех же исключительных индексах (13/5), что и в бенче 08.09. Остаток —
разница квантов (MLX 4-bit vs GGUF Q4_K_M), не промпта. llama top-10 побайтово
совпал с отчетом 08.09 (стабильность), токены: llama 11162 vs MLX 8748 (cap850) —
шаблоны все еще не байт-идентичны.

Вывод для выбора бэкенда: llama быстрее на коротких (first pass 850), MLX ~2x быстрее
на длинных (second pass 2400). Смешанная маршрутизация — кандидат, но требует
проверки качества на retrieval (hit@10), а не только overlap скоров: флипы через
порог 0.3 остаются.

## Честные оговорки

- Каталог пересобран: 59579 items сейчас vs 136578 в отчете 06.09. Кейсы те же
  (Python hit 0.952 в обоих прогонах — стабилен), но строгое сравнение с 09.09
  только индикативное.
- `restored-run30` гонял Rust-руку на MLX, новый прогон — обе руки на llama.
  Часть прироста — эффект общего CE, не только parity-фиксов.
- n=21, не 1065. Полный прогон — следующий шаг.
- Остаточный гэп MRR −12.7pp: кандидаты `fan_in` заглушка 1.0, Hub prior не
  портирован, нет preserve_vector_top/kind-limits/family-penalty.

## Воспроизводимость

- Бинарь: `f84870ab3362396880422460e611bbd877dab6599560994ecec2363fb9a304d1`
  (`cargo build --release`, 0 warnings, 59 тестов).
- Пин `freeze()` в `scripts/research_rust_full_eval.py` обновлен: `ab54768c...`
  (не соответствовал ни одному собранному бинарю) → `f84870ab...`.
- Сервисы на момент прогона: embedder `:8001` (`serve_embedder.sh`, 200),
  llama CE `:18081` (batch 2048, ctx 40960, `--pooling rank --reranking`),
  Qdrant `:6333`, MLX CE `:18083` (после бенча).
- Сырые эвиденсы (gitignored): `artifacts/research/2026-09-07_rust-full-eval/2026-09-21_h91afix-{smoke3,run21}/`,
  `artifacts/research/2026-09-21_ce-backend-bench/results.json`.
- Починки `scripts/serve_mlx_ce.sh`: пустой массив под `set -u`, loopback-пины
  против WARP-hang (`VLLM_HOST_IP`, `GLOO_SOCKET_IFNAME`).
